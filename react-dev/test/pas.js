describe('move', () => {
    beforeEach(function() {
        browser.ignoreSynchronization = true;
        browser.sleep(10000);
    });
    it ('Obtain info',function() {
       obtainLocation();
       //nextpag();
      });
  });

 
    